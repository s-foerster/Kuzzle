{
  "name": "hearts-game",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server.js",
    "server:dev": "node --watch server.js",
    "full": "concurrently \"npm run dev\" \"npm run server:dev\"",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "vue": "^3.4.15",
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.3",
    "gh-pages": "^6.1.1",
    "vite": "^5.0.11",
    "concurrently": "^8.2.2"
  }
}
