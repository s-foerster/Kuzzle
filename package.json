{
  "name": "hearts-game",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node server.js",
    "server:dev": "node --watch server.js",
    "full": "concurrently \"npm run dev\" \"npm run server:dev\"",
    "deploy": "cross-env DEPLOY_TARGET=github npm run build && gh-pages -d dist",
    "build:koyeb": "npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "vue": "^3.4.15"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.3",
    "concurrently": "^8.2.2",
    "cross-env": "^10.1.0",
    "gh-pages": "^6.1.1",
    "vite": "^5.0.11"
  }
}
